13
T
T F
( \rightarrow ( T ) F )
( \lor a b ) \land
( \neg a )
( \leftrightarrow ( \neg a b ) b )
( \land ( \rightarrow ( \neg a ) ( \neg ( \land a y ) ) ) ( \lor a b ) )
( \land ( \rightarrow ( \neg a ) ( \neg ( \land a y ) ) ( \lor a b ) )
( \land a b )
( \land )
( \neg ( \lor a b ) )
( \neg ( \rightarrow ( \neg ( a ) ) ( \land a b ) ) )
( \neg ( \neg T ) )